(function () {
  if (!document.querySelector('.gallery-v2') || !window.v2Motion) return;
  // Both galleries are fan-carousels now (fan-carousel.js owns their
  // interaction); nothing extra to wire here currently.
})();
