_site/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/
.sass-cache/
# Gemfile.lock is intentionally committed -- it pins exact gem versions so
# the GitHub Actions build reproduces the same site every time.
